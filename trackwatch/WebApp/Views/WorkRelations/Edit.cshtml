@model Domain.App.WorkRelation

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>WorkRelation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="WorkId" class="control-label"></label>
                <select asp-for="WorkId" class="form-control" asp-items="ViewBag.WorkId"></select>
                <span asp-validation-for="WorkId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RelatedWorkId" class="control-label"></label>
                <select asp-for="RelatedWorkId" class="form-control" asp-items="ViewBag.RelatedWorkId"></select>
                <span asp-validation-for="RelatedWorkId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
